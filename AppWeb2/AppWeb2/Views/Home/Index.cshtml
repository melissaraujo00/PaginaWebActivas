@model IEnumerable<AppWeb2.Models.VideoJuego>
@{
    ViewData["Title"] = "Videojuegos";
}


<div class="container">
    @await Html.PartialAsync("_FeatureCards")
</div>
<div class="container mt-5">



    <h2 class="text-center fw-bold mb-4" style="color: #FFD700;"> Videojuegos Disponibles</h2>
    <div class="row g-4">
        @foreach (var juego in Model)
        {
            <div class="col-md-6 col-lg-4 col-xl-3">
                <div class="card game-card h-100">


                    <div class="badge-top">+@juego.EdadMinima</div>

 
                    <div class="favorite">
                        <i class="fa-regular fa-heart"></i>
                    </div>


                    <img src="@juego.imagen"
                         class="card-img-top game-img" />

                    <div class="card-body d-flex flex-column">

                        <h5 class="card-title fw-bold">
                            @juego.Titulo
                        </h5>

                        <h4 class="price mt-2">
                            $@juego.Precio
                        </h4>


                        <div class="d-flex align-items-center mt-auto">
                            <i class="fa-solid fa-gamepad me-2 text-warning"></i>
                        </div>

                    </div>
                </div>
            </div>
        }
    </div>

</div>